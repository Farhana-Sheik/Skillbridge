# Frontend environment variables for Vite
# Leave empty for local dev (vite dev server proxies /api to backend)
# For production set the backend API base URL ending with /api
# Example: VITE_API_URL=https://your-backend.example.com/api
VITE_API_URL=
